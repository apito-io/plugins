# Plugin source lives in dedicated GitHub repos, never in this registry.
/hc-*-plugin/
/hc-*-plugin-1/

# Go
*.test
*.out
vendor/

# Node / UI
node_modules/
**/ui/dist/
*.umd.js
package-lock.json
pnpm-lock.yaml

# Signed catalog (generated; commit these)
!dist/
!dist/catalog.json
!dist/catalog.sig
!dist/plugin-catalog.schema.json


# IDE
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Debug
*.log
debug/

# env files tracked for single-dev monorepo setup (do not re-ignore .env / .env.local)
.env.*.local
